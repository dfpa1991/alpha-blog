<h1>Showing article details</h1>

<!--To Evaluate ruby inside HTML the tag <% %> is used and to display it the tag <%= %> is used-->
<p><strong>Title: </strong><%= @article.title %></p>
<p><strong>Description: </strong><%= @article.description %></p>
<%= link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Return to articles listing', articles_path %>
<td><%= button_to "Delete", article_path(@article), method: :delete,
                  data: { turbo_confirm: "Are you sure you want to delete this article #{@article.title}?" }%></td>
